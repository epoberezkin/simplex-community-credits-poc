node_modules
dist
build
artifacts
artifacts-pvm
cache
typechain-types
*.log
.env
.env.*
!.env.example

# Circuit build outputs
circuits/build
circuits/keys

# Trusted setup artifacts (downloaded, large)
ptau/*.ptau

# Chopsticks state
chopsticks/*.sqlite
chopsticks/*.sqlite-*
.idea/

# browser-e2e (Playwright)
test/browser/.runtime/
test/browser/test-results/
test/browser/playwright-report/

# Per-deploy manifests + dapp configs (regenerated by tools/deploy.mjs)
tools/last-deploy.json
packages/*/public/config.json
